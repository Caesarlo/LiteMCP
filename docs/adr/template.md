# ADR-NNNN: <标题，动词短语，如"采用 X 而非 Y">

- Status: Proposed | Accepted | Superseded by ADR-NNNN | Deprecated
- Date: YYYY-MM-DD
- Source refs: <docs/architecture/xx-*.md#section 或行号>

## Context

决策发生的背景、约束和已知选项。说明触发这次决策的问题，以及为什么现在需要决定（而不是留到以后）。如果这个决策依赖某个更早的 ADR 或架构文档章节，在这里链接。

## Decision

明确陈述做出的决定。用主动语态："我们将……"而不是"我们可能……"。如果同时排除了某些候选方案，列出被拒绝的方案和拒绝理由，避免日后重新提出同一个已否决的方案。

## Consequences

这个决定带来的正面和负面后果、后续必须联动修改的文档/代码、需要监控的风险，以及触发重新评估这条决策的条件（例如"如果 X 发生变化，需重新评估本 ADR"）。如果决定中有明确标记为 [建议]/[后续] 的暂缓部分，在这里说明触发条件。
